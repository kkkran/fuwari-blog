---
import path from "node:path";
import type { CollectionEntry } from "astro:content";
import { render } from "astro:content";
import Icon from "@components/Icon.astro";

import { serviceConfig } from "../config";
import { formatPostDateForDisplay } from "../utils/date-utils";
import { getDir } from "../utils/url-utils";
import PostMetadata from "./PostMeta.astro";
import ImageWrapper from "./misc/ImageWrapper.astro";

interface Props {
	class?: string;
	entry: CollectionEntry<"posts">;
	title: string;
	url: string;
	published: Date;
	updated?: Date;
	image: string;
	description: string;
	draft: boolean;
	style: string;
	loading?: "eager" | "lazy";
}
const {
	entry,
	title,
	url,
	published,
	updated,
	image,
	description,
	style,
	loading = "lazy",
} = Astro.props;

const isPinned = entry.data.pinned === true;
const className = Astro.props.class;

const hasCover = image !== undefined && image !== null && image !== "";

const coverWidth = "28%";

const { remarkPluginFrontmatter } = await render(entry);
---
<a
    href={url}
    aria-label={title}
    class:list={["group card-base card-hover flex flex-col w-full relative p-5 md:p-6", className]}
    style={style}
>
    <div class:list={["relative min-w-0", {"pr-16": !hasCover, "md:w-[calc(100%_-_var(--coverWidth)_-_1rem)] md:pr-4": hasCover}]}>
        <div class="mb-3 flex items-center gap-2 text-[11px] uppercase tracking-[0.16em] text-white/35">
            {isPinned && <span class="rounded-md border border-[var(--primary)] px-2 py-0.5 text-[var(--primary)]">Pinned</span>}
            <span>Note</span>
        </div>
        <h2
            class="transition w-full block font-bold mb-2 md:mb-3 text-xl md:text-2xl text-90 relative leading-tight
        group-hover:text-[var(--primary)]
        group-active:text-[var(--title-active)]
        "
        >
            {title}
        </h2>

        <!-- description -->
        <div class:list={["transition text-75 mb-4 pr-4 line-clamp-2 leading-relaxed"]}>
            { description || remarkPluginFrontmatter.excerpt }
        </div>

        <!-- metadata -->
        <PostMetadata
            published={published}
            updated={updated}
            hideUpdateDate={true}
            hidePublishedDate={false}
            slug={entry.id}
            showTime={false}
            class="mb-2 md:mb-4"
        ></PostMetadata>

        <!-- word count, read time and page views -->
        <div class="text-sm text-white/35 flex gap-4 transition">
            <div>{remarkPluginFrontmatter.words} 字</div>
            <div>|</div>
            <div>{remarkPluginFrontmatter.minutes} 分钟</div>
        </div>

    </div>

    {hasCover && <div
                    class:list={[
                        "mt-4 md:mt-0",
                        "md:w-[var(--coverWidth)] md:absolute md:top-5 md:bottom-5 md:right-5 overflow-hidden rounded-lg border border-white/10 active:scale-[0.99] flex items-center"
                    ]} >
        <div class="absolute pointer-events-none z-10 w-full h-full group-hover:bg-black/20 group-active:bg-black/35 transition"></div>
        <!-- 封面图上的箭头 -->
        <div class="absolute pointer-events-none z-20 w-full h-full flex items-center justify-center ">
            <Icon name="material-symbols:chevron-right-rounded"
                  class="transition opacity-0 group-hover:opacity-100 scale-50 group-hover:scale-100 text-white text-5xl">
            </Icon>
        </div>
        <ImageWrapper src={image} basePath={path.join("content/posts/", getDir(entry.id))} alt="Cover Image of the Post"
                  class="w-full min-h-0" loading={loading} objectFit="contain">
        </ImageWrapper>
    </div>}

    {!hasCover &&
        <div class="!flex btn-regular w-11
         absolute right-5 top-5 bottom-5 bg-[var(--enter-btn-bg)]
         group-hover:bg-[var(--enter-btn-bg-hover)] group-active:bg-[var(--enter-btn-bg-active)] active:scale-95
        ">
            <Icon name="material-symbols:chevron-right-rounded"
                  class="transition text-white/80 text-3xl mx-auto">
            </Icon>
        </div>
    }
</a>



<style define:vars={{coverWidth}}>
</style>

<script define:vars={{ assetsBaseUrl: serviceConfig.assetsBaseUrl }}>
	(function() {
		const covers = document.querySelectorAll('.post-cover-random');
		if (!covers.length) return;
		const max = 1176;
		for (const img of covers) {
			const n = Math.floor(Math.random() * max) + 1;
			img.src = `${assetsBaseUrl}/ri/h/${n}.webp`;
		}
	})();

</script>
